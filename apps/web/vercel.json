{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "npx prisma generate && npx prisma migrate deploy && next build",
  "installCommand": "pnpm install",
  "devCommand": "next dev -p 8787"
}
